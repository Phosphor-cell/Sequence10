# .github/workflows/keep-alive.yml
name: Keep Vercel Warm
on:
  schedule:
    - cron: '*/5 * * * *'  # every 5 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -f https://sequence10.vercel.app/api/health || exit 1